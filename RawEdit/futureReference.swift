//
//  futureReference.swift
//  RawEdit
//
//  Created by Ege on 29.11.17.
//  Copyright © 2017 Ege. All rights reserved.
//

    /*
import Foundation


changeColor () {
        for basic color adjustments
        guard let colorControls = CIFilter(name: "CIColorControls") else{return nil}
       shadow highlight control from apple
        doesnt really work with linearspace filter
        guard let toneCurve = CIFilter(name: "CIToneCurve") else{return nil}

 colorControls.setValue(rawImage.outputImage, forKey: kCIInputImageKey)
 colorControls.setValue(1.2, forKey: kCIInputSaturationKey)
 colorControls.setValue(0.0, forKey: kCIInputBrightnessKey)
 colorControls.setValue(1, forKey: kCIInputContrastKey)
 
 let vector0 = CIVector(x: 0.47, y: 0.0)
 let vector1 = CIVector(x: 0.997, y: 1)
 let vector2 = CIVector(x: 0.998, y: 1)
 let vector3 = CIVector(x: 0.999, y: 1)
 let vector4 = CIVector(x: 1, y: 1)
 
 toneCurve.setValue(rawImage.outputImage, forKey: kCIInputImageKey)
 toneCurve.setValue(vector0, forKey: "inputPoint0")
 toneCurve.setValue(vector1, forKey: "inputPoint1")
 toneCurve.setValue(vector2, forKey: "inputPoint2")
 toneCurve.setValue(vector3, forKey: "inputPoint3")
 toneCurve.setValue(vector4, forKey: "inputPoint4")
 
 if let output = toneCurve.value(forKey: kCIOutputImageKey) as? CIImage{
 return output
 }
}
 */

